# 設計ワークシート（記入用）：要件シナリオ → サービス構成

各シナリオで7項目を埋め、構成図を描き、一言サマリを書く。
**先に自分で埋めてから** `scenario-worksheet-answers.md` で答え合わせをする。
記入の選択肢は L1-2-3 座学の決定フロー（§4）と各章を参照。

選択肢の早見：

| # | 設計項目 | 選択肢 |
|---|---|---|
| ① | グラウンディングの要否 | `不要（モデル単体）` / `必要（RAG）` |
| ② | 検索方式 | `keyword` / `vector` / `semantic` / `hybrid`（迷ったら semantic hybrid） |
| ③ | インデックス方式 | `integrated vectorization＋indexer` / `前処理で markdown 化` / `出典フィールドを持つ` |
| ④ | グラウンディング手段 | `AI Search 自作(classic RAG)` / `File Search` / `Azure AI Search ツール` / `Foundry IQ` |
| ⑤ | メモリ | `会話メモリ(短期)` / `Memory store(長期・プレビュー)` |
| ⑥ | ツール | built-in（`Web search` / `Code Interpreter` / `File Search` / `Azure AI Search` / `Function calling` / `Azure Functions`）／ custom（`MCP` / `OpenAPI` / `A2A` / `Toolbox`） |
| ⑦ | 認証 | `Microsoft Entra(キーレス)` 優先 ／ 各ソースは `接続(connection)` |

> 正解は1つではない。**要件に対して理由を言えるか**が評価軸。理由の欄を空けたまま先に進まないこと。

---

## シナリオA：社内QA エージェント

**要件**：従業員が「育休は何日取れる？」「経費精算の締めは？」と自然文で質問する。答えは **SharePoint の社内規程**と **Blob Storage の人事PDF**に基づく。**人事情報は職位で見せてよい範囲が違う**（権限考慮が必須）。同じ知識を**複数のエージェント**（人事Bot・総務Bot）で使い回したい。

| 項目 | あなたの選定 | 理由 |
|---|---|---|
| ① グラウンディングの要否 |  |  |
| ② 検索方式 |  |  |
| ③ インデックス方式 |  |  |
| ④ グラウンディング手段 |  |  |
| ⑤ メモリ |  |  |
| ⑥ ツール |  |  |
| ⑦ 認証 |  |  |

構成図：（手描き・draw.io・PowerPoint など何でもよい。描いた画像をここに貼る）

一言サマリ：

---

## シナリオB：文書要約・論点抽出

**要件**：法務が、契約PDFの山から「自動更新条項のある契約だけ要約して」のように指示する。**単一の文書集合**で、権限はシンプル。**GA（正式提供）の機能だけ**で素早く出したい。会話で深掘り（「その中で解約30日前通知のものは？」）もしたい。

| 項目 | あなたの選定 | 理由 |
|---|---|---|
| ① グラウンディングの要否 |  |  |
| ② 検索方式 |  |  |
| ③ インデックス方式 |  |  |
| ④ グラウンディング手段 |  |  |
| ⑤ メモリ |  |  |
| ⑥ ツール |  |  |
| ⑦ 認証 |  |  |

構成図：

一言サマリ：

---

## シナリオC：マルチモーダル問い合わせ

**要件**：「この画像の製品の保証期間は？」のように、**製品画像と仕様PDFの混在**コンテンツに答える。画像の中身（型番ラベル・外観）も理解したい。

| 項目 | あなたの選定 | 理由 |
|---|---|---|
| ① グラウンディングの要否 |  |  |
| ② 検索方式 |  |  |
| ③ インデックス方式 |  |  |
| ④ グラウンディング手段 |  |  |
| ⑤ メモリ |  |  |
| ⑥ ツール |  |  |
| ⑦ 認証 |  |  |

構成図：

一言サマリ：

---

## 振り返り（埋め終わってから書く）

- 3つのシナリオは、同じ7項目を埋めたのに **④ グラウンディング手段の着地が全部違う**はず。どこで分かれたか、自分の言葉で書く：
- 「過剰設計（何でも Foundry IQ）」や「不足（厳密一致が要るのに純 vector）」に陥っていないか：
