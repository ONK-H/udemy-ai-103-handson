# Translator リソースのエンドポイント（ポータルの「概要」からコピー。末尾のスラッシュは無くてOK）
TRANSLATOR_ENDPOINT=https://<your-translator-resource>.cognitiveservices.azure.com

# キー認証を使うときだけ設定（今回はキーレスが既定なので空のままでよい）
TRANSLATOR_KEY=
TRANSLATOR_REGION=

# LLM でトーン制御するときに使うデプロイ名（カタログ名ではなく「デプロイ名」）
LLM_DEPLOYMENT=gpt-5.4-nano
